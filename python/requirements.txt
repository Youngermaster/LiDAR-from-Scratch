# =====================================================================
# Python dependencies for the experiments in this directory.
# Pinned loosely; bump them as needed and update python/README.md.
#
# All packages are intentionally small and well-known. Do not add web
# frameworks, ML libraries, or data science stacks here. See CLAUDE.md
# section "Minimal dependencies".
# =====================================================================

# Serial-port abstraction. Used by the from-scratch driver in
# python/lib/rplidar_c1.py to talk to the LiDAR's USB-serial adapter.
pyserial>=3.5,<4.0

# Numerical work: polar-to-cartesian, smoothing buffers, simple math.
numpy>=1.26,<3.0

# Visualization for live plots and CSV replay.
matplotlib>=3.8,<4.0

# Note: we deliberately do NOT depend on `pyrplidar` or `rplidar`.
# Their decoders are incorrect for the C1's firmware (firmware string
# missing in INFO, "index out of range" in scan-node parsing,
# us_per_sample left in Q8 fixed point). We implement the small subset
# of the SLAMTEC serial protocol we actually need in lib/rplidar_c1.py.
# The protocol is documented in docs/protocol-notes.md.
